const config = require("./utils/config");
const logger = require("./utils/logger");
const app = require("./app");

// TODO: Implement function that connects the express server and the mongodb database
